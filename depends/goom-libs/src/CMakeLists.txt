add_subdirectory(vivid)
add_subdirectory(goom)

if (NOT ENABLE_TESTING)
    message(STATUS "Goom-libs: NOT Building Unit Tests.")
else ()
    enable_testing()
    message(STATUS "Goom-libs: Building Unit Tests.")
    add_subdirectory(tests)
endif ()
